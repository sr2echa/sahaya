{
  "name": "backend-in-next",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "backend-in-next",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/backend-in-next"
      },
      "configurations": {
        "development": {
          "outputPath": "backend-in-next"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "backend-in-next:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "backend-in-next:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "backend-in-next:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nx/next:export",
      "options": {
        "buildTarget": "backend-in-next:build:production"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "backend-in-next/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    }
  },
  "tags": []
}
